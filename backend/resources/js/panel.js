// Entrada JS del panel Blade para @vite: reusa el pegamento vanilla compartido del frontend
// (flash de sesión → toast, confirmar antes de enviar, menús de acciones). Fuente única en frontend/assets.
import '@shared/js/panel.js';
